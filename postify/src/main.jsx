import React from 'react'
import ReactDOM from 'react-dom/client'
import PostifyApp from './PostifyApp.jsx'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <PostifyApp />
  </React.StrictMode>,
)
